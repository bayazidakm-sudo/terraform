trigger:
  - main

variables:
  - name: serviceConnection
    value: 'CandidateProject-2'
  - name: tfworkingDirectory
    value: '$(System.DefaultWorkingDirectory)'
  - name: environment
    value: 'prod'
  # Azure authentication variables
  - name: ARM_CLIENT_ID
    value: '$(servicePrincipalId)'
  - name: ARM_CLIENT_SECRET
    value: '$(servicePrincipalKey)'
  - name: ARM_SUBSCRIPTION_ID
    value: '$(subscriptionId)'
  - name: ARM_TENANT_ID
    value: '$(tenantId)'

stages:
- stage: SecurityScan
  jobs:
  - job: TerraformScan
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    # Create terraform directory if needed
    - script: |
        set -euo pipefail
        mkdir -p $(System.DefaultWorkingDirectory)/terraform
        cp -r * $(System.DefaultWorkingDirectory)/terraform/ || true
      displayName: 'Setup Terraform Directory'
    
    # Verify workspace structure
    - script: |
        set -euo pipefail
        echo "Workspace structure:"
        pwd
        ls -R $(System.DefaultWorkingDirectory)
      displayName: 'Verify Workspace Structure'
    
    # Install Terraform using official method
    - script: |
        set -euo pipefail
        echo "Installing HashiCorp GPG key..."
        wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg > /dev/null
        
        echo "Adding HashiCorp repository..."
        echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list > /dev/null
        
        echo "Updating package list..."
        sudo apt-get update
        
        echo "Installing specific version of Terraform..."
        sudo apt-get install -y terraform=1.13.4
        
        echo "Verifying Terraform installation..."
        terraform version || {
          echo "Terraform installation failed"
          exit 1
        }
        
        # Ensure terraform is in PATH and executable
        which terraform
        ls -l $(which terraform)
      displayName: 'Install Terraform'
    
    - script: |
        set -euo pipefail
        TFSEC_URL="https://github.com/aquasecurity/tfsec/releases/latest/download/tfsec-linux-amd64"
        echo "Downloading tfsec from $TFSEC_URL"
        # -f fails on HTTP errors, -S shows errors, -L follows redirects
        curl -fSL -o tfsec "$TFSEC_URL"
        if [ ! -s tfsec ]; then
          echo "tfsec binary not downloaded or empty" >&2
          exit 1
        fi
        chmod +x tfsec
        echo "tfsec version: $(./tfsec --version || echo 'unknown')"
        
        # Create initial XML structure
        cat > test-results.xml << "EOF"
<?xml version="1.0" encoding="UTF-8"?>
<testsuites>
  <testsuite name="tfsec" tests="0" failures="0">
  </testsuite>
</testsuites>
EOF
        
        # Run tfsec and capture both output and exit code
        set +e
        OUTPUT=$(./tfsec "$(tfworkingDirectory)" --format junit 2>&1)
        TFSEC_EXIT_CODE=$?
        set -e
        
        # Verify XML is valid before publishing
        if xmllint --noout test-results.xml.tmp 2>/dev/null; then
          mv test-results.xml.tmp test-results.xml
          echo "Valid JUnit XML generated"
        else
          # Generate a valid XML with error message if tfsec output is invalid
          cat > test-results.xml << EOF
<?xml version="1.0" encoding="UTF-8"?>
<testsuites>
  <testsuite name="tfsec" tests="1" failures="1">
    <testcase name="tfsec_scan" classname="SecurityScan">
      <failure message="Failed to generate valid tfsec output">Check terraform directory manually</failure>
    </testcase>
  </testsuite>
</testsuites>
EOF
          echo "Generated fallback XML due to invalid tfsec output"
        fi
        
        echo "Final output files:"
        ls -l tfsec test-results.xml
      displayName: 'Run tfsec Security Scanner'
    
    - script: |
        set -euo pipefail
        echo "Checking for test-results.xml file..."
        if [ -f "test-results.xml" ]; then
          echo "test-results.xml found. Listing details:"
          ls -l test-results.xml
        else
          echo "test-results.xml not found!"
          exit 1
        fi
      displayName: 'Verify Test Results File'
      condition: succeededOrFailed()

    - task: PublishTestResults@2
      inputs:
        testResultsFormat: 'JUnit'
        testResultsFiles: '**/test-results.xml'
        testRunTitle: 'Terraform Security Scan'
      condition: succeededOrFailed()

- stage: Validate
  jobs:
  - job: ValidateAndPlan
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    # Install Terraform using HashiCorp's official repository
    - script: |
        set -euo pipefail
        echo "Installing HashiCorp GPG key..."
        wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
        
        echo "Adding HashiCorp repository..."
        echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
        
        echo "Updating package list and installing Terraform..."
        sudo apt-get update && sudo apt-get install -y terraform
        
        echo "Verifying Terraform installation..."
        terraform version
      displayName: 'Install Terraform'
    
    - script: |
        set -euo pipefail
        
        # Check workspace directory
        echo "Workspace directory contents:"
        ls -la $(System.DefaultWorkingDirectory)
        
        # Verify working directory
        echo "Current working directory: $PWD"
        echo "Target working directory: $(tfworkingDirectory)"
        
        # Initialize Terraform with backend config
        terraform init \
          -backend-config="resource_group_name=webapp-rg1" \
          -backend-config="storage_account_name=mytfstatefiles1" \
          -backend-config="container_name=tfstate" \
          -backend-config="key=terraform.tfstate" \
          -backend-config="subscription_id=$(ARM_SUBSCRIPTION_ID)" \
          -backend-config="tenant_id=$(ARM_TENANT_ID)" \
          -backend-config="client_id=$(ARM_CLIENT_ID)" \
          -backend-config="client_secret=$(ARM_CLIENT_SECRET)"
      displayName: 'Terraform Init'
      env:
        # Using service principal authentication
        ARM_CLIENT_ID: $(ARM_CLIENT_ID)
        ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
        ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
        ARM_TENANT_ID: $(ARM_TENANT_ID)
    
    - script: |
        set -euo pipefail
        cd $(tfworkingDirectory)
        terraform validate
      displayName: 'Terraform Validate'
      env:
        ARM_CLIENT_ID: $(ARM_CLIENT_ID)
        ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
        ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
        ARM_TENANT_ID: $(ARM_TENANT_ID)
    
    - script: |
        set -euo pipefail
        cd $(tfworkingDirectory)
        terraform plan -out=$(Build.ArtifactStagingDirectory)/terraform.tfplan
      displayName: 'Terraform Plan'
      env:
        ARM_CLIENT_ID: $(ARM_CLIENT_ID)
        ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
        ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
        ARM_TENANT_ID: $(ARM_TENANT_ID)
    
    - publish: $(Build.ArtifactStagingDirectory)/terraform.tfplan
      artifact: TerraformPlan

- stage: Deploy
  dependsOn: 
  - SecurityScan
  - Validate
  condition: succeeded()
  jobs:
  - deployment: ApplyTerraform
    environment: $(environment)
    pool:
      vmImage: 'ubuntu-latest'
    strategy:
      runOnce:
        deploy:
          steps:
          # Install Terraform using HashiCorp's official repository
          - script: |
              set -euo pipefail
              echo "Installing HashiCorp GPG key..."
              wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
              
              echo "Adding HashiCorp repository..."
              echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
              
              echo "Updating package list and installing Terraform..."
              sudo apt-get update && sudo apt-get install -y terraform
              
              echo "Verifying Terraform installation..."
              terraform version
            displayName: 'Install Terraform'
          
          - script: |
              set -euo pipefail
              cd $(tfworkingDirectory)
              
              # Initialize Terraform with backend config
              terraform init \
                -backend-config="resource_group_name=webapp-rg1" \
                -backend-config="storage_account_name=mytfstatefiles1" \
                -backend-config="container_name=tfstate" \
                -backend-config="key=terraform.tfstate"
            displayName: 'Terraform Init'
            env:
              ARM_CLIENT_ID: $(ARM_CLIENT_ID)
              ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
              ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
              ARM_TENANT_ID: $(ARM_TENANT_ID)
          
          - download: current
            artifact: TerraformPlan

          - script: |
              set -euo pipefail
              cd $(tfworkingDirectory)
              terraform apply -auto-approve $(Pipeline.Workspace)/TerraformPlan/terraform.tfplan
            displayName: 'Terraform Apply'
            env:
              ARM_CLIENT_ID: $(ARM_CLIENT_ID)
              ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
              ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
              ARM_TENANT_ID: $(ARM_TENANT_ID)

- stage: SecurityValidation
  dependsOn: Deploy
  condition: succeeded()
  jobs:
  - job: RunSecurityScans
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - task: AzurePowerShell@5
      inputs:
        azureSubscription: '$(serviceConnection)'
        ScriptType: 'InlineScript'
        Inline: |
          # Enable Microsoft Defender for Cloud
          Set-AzSecurityPricing -Name "VirtualMachines" -PricingTier "Standard"
          
          # Get Security Assessment
          $assessment = Get-AzSecurityAssessment -ResourceGroupName "$(resourceGroupName)"
          $assessment | ConvertTo-Json | Out-File "security-assessment.json"
        azurePowershellVersion: 'LatestVersion'
    
    - publish: security-assessment.json
      artifact: SecurityAssessment
